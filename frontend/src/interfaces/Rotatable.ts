export default interface Rotatable {
    /**
     * Rotate the object by the given angle in degrees.
     * @param angle The angle to rotate the object by in degrees.
     * @returns void
     */
    rotate(angle: number): void;
}
